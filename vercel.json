{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "echo \"frontend-only — API runs on whitelisted VPS\"",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
