# Python backend (reference only)

This is the **original FastAPI implementation**, kept for reference. It is **not used in production**.

The live backend is **Node.js + Express** in [`/backend`](../backend/).
